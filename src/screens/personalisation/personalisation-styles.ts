import { StyleSheet } from 'react-native';
import theme from '../../theme';

const styles: any = StyleSheet.create({
  button: {
    position: 'absolute',
    bottom: theme.margins.external,
    left: theme.margins.external,
    right: theme.margins.external
  }
});

export default styles;
