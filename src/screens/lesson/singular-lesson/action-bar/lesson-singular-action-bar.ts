import { StyleSheet } from 'react-native';

const styles: any = StyleSheet.create({
  actionBar: {
    display: 'flex',
    flexDirection: 'row'
  },
  actionText: {
    paddingLeft: 16
  }
});

export default styles;
