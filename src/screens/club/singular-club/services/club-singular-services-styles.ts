import { StyleSheet } from 'react-native';

const styles: any = StyleSheet.create({
  servicesContainer: {
    flexDirection: 'row',
    flexWrap: 'wrap'
  },
  tag: {
    marginRight: 8,
    marginVertical: 4
  }
});

export default styles;
