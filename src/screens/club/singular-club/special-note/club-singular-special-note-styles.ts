import { StyleSheet } from 'react-native';

const styles: any = StyleSheet.create({
  container: {
    backgroundColor: '#ECC3B2', // This color is only used here
    paddingVertical: 8,
    paddingHorizontal: 16
  },
  fullTextContainer: {
    flexWrap: 'nowrap',
    flex: 1,
    flexDirection: 'row'
  }
});

export default styles;
