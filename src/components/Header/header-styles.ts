import { StyleSheet } from 'react-native';

const styles: any = StyleSheet.create({
  headerContainer: {
    flexDirection: 'row'
  },
  item: {
    width: 40, // 24 + (8 + 8 margins)
    justifyContent: 'center',
    alignItems: 'center'
  }
});

export default styles;
